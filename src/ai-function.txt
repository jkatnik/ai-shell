function ai() {
  echo "aborted" > /tmp/ai-autocomplete.out
  history -s "ai $@"
  ai-autocomplete "$@"

  COMMAND="$(< /tmp/ai-autocomplete.out)"
  if [ "$COMMAND" != "aborted" ]; then
      eval "$COMMAND"
      history -s $COMMAND
  fi
}
